---
layout: "../layouts/BaseLayout.astro"
title: "About"
---

<!-- About Page v2 - Fantasy Themed -->
<section class="relative min-h-screen text-white overflow-hidden bg-black">
  <!-- 背景插画 -->
  <img src="/images/about-hero.png" alt="Mystical Forest" class="absolute inset-0 w-full h-full object-cover opacity-60 z-0">

  <!-- 半透明遮罩 -->
  <div class="absolute inset-0 bg-gradient-to-b from-black/70 via-purple-900/60 to-black/90 z-10 backdrop-blur-sm"></div>

  <!-- 正文内容 -->
  <div class="relative z-20 max-w-4xl mx-auto px-6 py-28 text-center">
    <h1 class="text-5xl md:text-6xl font-bold text-pink-200 drop-shadow mb-6 animate-fade-in">✦ About <span class="text-yellow-200">Fishnini</span> ✦</h1>
    <p class="text-lg md:text-xl text-purple-100 leading-relaxed mb-10 animate-slide-up">
      Fishnini is not a fantasy to escape — it's a portal into your forgotten self. A world where foxes dream in color, owls speak in riddles, and every light is a memory.
    </p>

    <!-- 水晶风格图卡 -->
    <div class="bg-white/10 border border-white/20 backdrop-blur-md rounded-3xl shadow-2xl p-8 max-w-3xl mx-auto animate-zoom-in">
      <p class="text-white/90 text-lg">
        ✧ In this enchanted glade, your heart remembers how to wander. <br>
        ✧ In every glowing stone, your spirit finds its name. <br>
        ✧ Welcome home.
      </p>
    </div>

    <!-- 返回顶部按钮 -->
    <div class="mt-16">
      <a href="#top" class="inline-block bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-600 text-white font-bold px-6 py-3 rounded-full shadow-lg hover:scale-105 transition-all">↑ Back to Top</a>
    </div>
  </div>
</section>

<style>
@keyframes fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}
@keyframes slide-up {
  from { transform: translateY(20px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}
@keyframes zoom-in {
  from { transform: scale(0.95); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}
.animate-fade-in {
  animation: fade-in 1s ease-out forwards;
}
.animate-slide-up {
  animation: slide-up 1.2s ease-out forwards;
}
.animate-zoom-in {
  animation: zoom-in 1.2s ease-out forwards;
}
</style>
